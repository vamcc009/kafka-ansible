dataDir={{ zookeeper.datadir }}
clientPort={{ zookeeper.port }}
clientPortAddress={{ kfk_server }}
maxClientCnxns=0
tickTime={{ zookeeper.tick_time }}
initLimit={{ zookeeper.init_limit }}
syncLimit={{ zookeeper.sync_limit }}
{% for server in groups["kafka_servers"] %}
server.{{ loop.index }}={{ server }}:2888:3888
{% endfor %}
